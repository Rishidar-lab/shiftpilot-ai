# Week 1 Submission Matrix — SHIFT PILOT

Statuses used in this document (and nowhere else in the repo):

- **PASS** — the requirement is satisfied and demonstrated by verifiable evidence in this
  repository (code, tests, CI workflow, or a reproducible script).
- **BLOCKED** — everything the repository can do without external input is done, but the
  requirement needs something this repo does not and cannot contain (credentials).
- **INCOMPLETE** — work remains that is within our control and not yet done.

Evidence in this file was checked against commit `8b94a56` and re-verified on 2026-08-14
(passes of `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm format`, `pnpm build`).

---

## Requirements

| #   | Week-1 requirement                                                | Status      | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Notes                                                                                                                                                     |
| --- | ----------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | Working product: the full workflow runs today                     | **PASS**    | Capture → review → approve → plan → handover all run offline end-to-end. `apps/api` (Fastify + SQLite) and `apps/web` (React SPA) boot with `pnpm dev` and zero configuration. 258 tests in 20 files, all green; CI runs the same gates.                                                                                                                                                                                                                                                                                                                                                                                    | Docs: `README.md`, `docs/architecture.md` §2.                                                                                                             |
| R2  | Real AI integration: a genuine AI interaction through the product | **BLOCKED** | `ClaudeProvider` (`packages/provider/src/claude.ts`) is a complete adapter: versioned prompt, structured output, typed failure mapping, timeout abort, bounded retries. `AI_PROVIDER=claude` boots it; health + UI report `Live AI`. The one missing piece is an **actual API call**, which requires `ANTHROPIC_API_KEY` + `ANTHROPIC_MODEL`. No live call has been made, so no fixture is labelled `"recorded"`.                                                                                                                                                                                                           | This is the only non-PASS row. When credentials exist, `ANTHROPIC_LIVE=1 AI_PROVIDER=claude pnpm eval:claude` produces the evidence and records fixtures. |
| R3  | Evaluation: how the AI's output is judged                         | **PASS**    | The trust model is the product's spine: provider output is untrusted; zod + domain policy + shift-local time resolution re-validate everything; humans approve before any task exists (`docs/architecture.md` §5–6). The 17-case eval corpus (`apps/api/src/eval/corpus.ts`) covers deadlines, dependencies, duplicates, vagueness, conflicts, injection and malformed input. `pnpm eval:claude` and `pnpm smoke:claude` run the corpus against a real model, record what happened per case, and deliberately compute **no accuracy percentage** (no labelled ground truth exists — a number would be invented confidence). | `docs/architecture.md` §6, `README.md` "Verifying a real Claude call".                                                                                    |
| R4  | CI: automated verification                                        | **PASS**    | `.github/workflows/ci.yml`: install → lint → typecheck → format → test → build, then a fresh-DB migration smoke test plus an idempotent re-migrate. `AI_PROVIDER=fake` is pinned in CI. CI needs **no secrets** and can never make a paid call (money-spending scripts require `ANTHROPIC_LIVE=1` + provider selection + key; `pnpm test` never imports them).                                                                                                                                                                                                                                                              |                                                                                                                                                           |
| R5  | Documentation                                                     | **PASS**    | `README.md` (quickstart, modes, trust model, cost controls, limitations), `docs/architecture.md` (602 lines: analysis, models, boundaries, failure cases), `docs/implementation-plan.md` (milestones + audit record), `CLAUDE.md` (engineering rules), `docs/claude-audit-handoff.md`. Verified consistent with the code that exists (2026-08-14 pass).                                                                                                                                                                                                                                                                     |                                                                                                                                                           |
| R6  | Demo: scripted, offline, cheap                                    | **PASS**    | `docs/demo-script.md` (2–4 minute walkthrough) + `docs/demo-seed-data.md` (one realistic shift). Runs entirely on `FakeAiProvider`: no network, no key, no cost, reproducible any time.                                                                                                                                                                                                                                                                                                                                                                                                                                     |                                                                                                                                                           |
| R7  | Interview readiness                                               | **PASS**    | `docs/interview-defense.md` (plain-language rationale for every design decision + difficult-question answers). `docs/rubric-self-review.md` scores the rubric conservatively against evidence.                                                                                                                                                                                                                                                                                                                                                                                                                              |                                                                                                                                                           |

## What the submission must NOT claim

- No fixture in this repository is labelled `"recorded"` — all six shipped fixtures are
  `"synthetic"`. Any description of a "real Claude call" in the submission must be marked as
  outstanding, not as done.
- The app has **no authentication** and the spend controls are a **brake, not a monetary
  cap**. Both are deliberate, documented Week-1 scopes (`README.md` "Limitations").
