# Week 1 Submission Matrix — ShiftPilot

Statuses used in this document (and nowhere else in the repo):

- **PASS** — the requirement is satisfied and demonstrated by verifiable evidence in this
  repository (code, tests, CI workflow, or a reproducible script).
- **INCOMPLETE** — the requirement needs an action outside this repository (external
  account, external platform, recording hardware) and is therefore part of the external
  submission checklist, not of the repo.

Evidence in this file was re-verified on 2026-08-15 against commit `d7167ce`: `pnpm lint`,
`pnpm typecheck`, `pnpm format`, `pnpm build` and `pnpm test` (**293 tests, 22 files**) all
green; production container built and smoke-tested, including recreation from an empty
database; live OpenRouter verification recorded in `docs/eval/` (`results.md` 16/16,
`verification-summary.md`, `live-eval-evidence.md`).

---

## Requirements

| #   | Week-1 requirement                                                | Status         | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Notes                                                                                                                                                               |
| --- | ----------------------------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | Working product: the full workflow runs today                     | **PASS**       | Capture → review → approve → plan → handover all run offline end-to-end. `apps/api` (Fastify + SQLite) and `apps/web` (React SPA) boot with `pnpm dev` and zero configuration. 283 tests in 21 files, all green; CI runs the same gates.                                                                                                                                                                                                                                                                                                                                                                                                                            | Docs: `README.md`, `docs/architecture.md` §2.                                                                                                                       |
| R2  | Real AI integration: a genuine AI interaction through the product | **PASS**       | **OpenRouter, free tier only**, live-verified 2026-08-14: one-request smoke (`pnpm smoke:openrouter`) returned HTTP 2xx; controlled evaluation (`pnpm eval:openrouter`) ran the full 16-case corpus + handover **16/16** on `google/gemma-4-26b-a4b-it:free`. Every request guarded by `assertFreeOpenRouterModel` (free ids only; paid models rejected; no paid fallback). Six `"source": "recorded"` fixtures capture the real responses. Reports: `docs/eval/results.md`, `docs/eval/verification-summary.md`, `docs/eval/live-eval-evidence.md`.                                                                                                                | Route: `AI_PROVIDER=openrouter`, `OPENROUTER_MODEL=openrouter/free` (smoke) or a specific `:free` model (eval). This is a controlled verification, not a benchmark. |
| R3  | Evaluation: how the AI's output is judged                         | **PASS**       | The trust model is the product's spine: provider output is untrusted; zod + domain policy + shift-local time resolution re-validate everything; humans approve before any task exists (`docs/architecture.md` §5–6). The 16-case eval corpus (`apps/api/src/eval/corpus.ts`) covers deadlines, dependencies, duplicates, vagueness, conflicts, injection and malformed input. `pnpm eval:openrouter` / `pnpm eval:claude` run the corpus against a real model, record what happened per case (attempts, resolved model, validation), and deliberately compute **no accuracy percentage** (no labelled ground truth exists — a number would be invented confidence). | `docs/architecture.md` §6, `README.md` "AI integration: OpenRouter".                                                                                                |
| R4  | CI: automated verification                                        | **PASS**       | `.github/workflows/ci.yml`: install → lint → typecheck → format → test → build, then a fresh-DB migration smoke test plus an idempotent re-migrate. `AI_PROVIDER=fake` is pinned in CI. CI needs **no secrets** and can never make a paid call (money-spending scripts require a key + provider selection; `pnpm test` never imports them).                                                                                                                                                                                                                                                                                                                         |                                                                                                                                                                     |
| R5  | Documentation                                                     | **PASS**       | `README.md` (quickstart, modes, trust model, OpenRouter verification, cost controls, limitations), `docs/architecture.md` (analysis, models, boundaries, failure cases, live-activation status), `docs/implementation-plan.md` (milestones + audit record), `CLAUDE.md` (engineering rules). Verified consistent with the code that exists (2026-08-15 pass).                                                                                                                                                                                                                                                                                                       |                                                                                                                                                                     |
| R6  | Demo: scripted, offline, cheap                                    | **PASS**       | `docs/demo-script.md` (2–4 minute walkthrough) + `docs/demo-seed-data.md` (one realistic shift). Runs entirely on `FakeAiProvider`: no network, no key, no cost, reproducible any time. Optionally demonstrable against the verified free OpenRouter route without showing any credential.                                                                                                                                                                                                                                                                                                                                                                          |                                                                                                                                                                     |
| R7  | Interview readiness                                               | **PASS**       | `docs/interview-defense.md` (plain-language rationale for every design decision + difficult-question answers, written against the OpenRouter-verified state). `docs/rubric-self-review.md` scores the rubric conservatively against evidence.                                                                                                                                                                                                                                                                                                                                                                                                                       |                                                                                                                                                                     |
| R8  | Public GitHub repository                                          | **PASS**       | Published at **https://github.com/Rishidar-lab/shiftpilot-ai** — public, MIT licensed, 13 topics, default branch `main`, CI green. Verified from the remote: no `.env`, no `*.db`, no `dist/`, no `node_modules/` among the tracked files; secret scan clean across tree, index and full history.                                                                                                                                                                                                                                                                                                                                                                   |                                                                                                                                                                     |
| R12 | Live deployment (supplementary)                                   | **INCOMPLETE** | Deployment package is complete and verified locally against the exact production artifact: single-service image (React at `/`, Fastify at `/api/*`), `render.yaml` Blueprint (free tier, Docker, `/api/health` health check, no secret values), compiled migration runner, `0.0.0.0` bind, injected `$PORT`, ephemeral SQLite at `/tmp` proven survivable by destroying and recreating the container. Creating the Render service and entering a **new rotated** `OPENROUTER_API_KEY` are external actions.                                                                                                                                                         | `docs/deployment.md` §8, `render.yaml`, `docs/post-deploy-checklist.md`.                                                                                            |
| R9  | Demo video upload                                                 | **INCOMPLETE** | Script and seed data are final (`docs/demo-script.md`, `docs/demo-seed-data.md`); recording requires hardware + an account.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | External checklist item 2.                                                                                                                                          |
| R10 | LinkedIn publication with Innovation Hacks tag                    | **INCOMPLETE** | Draft is final (`docs/linkedin-post.md`, placeholders for repo/video URLs). Posting requires the account.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | External checklist item 3.                                                                                                                                          |
| R11 | Final submission                                                  | **INCOMPLETE** | All evidence assembled; submission is an external action.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | External checklist item 4.                                                                                                                                          |

## What the submission must NOT claim

- **No claim that Claude is verified.** The Claude adapter exists but has never made a
  live call; the verified provider is OpenRouter on the free tier.
- **No accuracy percentages.** The 16/16 is a controlled corpus outcome, not a benchmark.
- **No fixed underlying model on `openrouter/free`** — the alias rotates across the free
  pool; the eval used an explicitly configured `google/gemma-4-26b-a4b-it:free`.
- **No monetary guarantees.** Spend controls are a brake, not a cap; there is no
  "production-ready" claim and no authentication — all deliberate, documented Week-1
  scopes (`README.md` "Current limitations").

## External checklist (the four actions left, in order)

1. Rotate the OpenRouter key used during verification (it was present on this machine
   during live runs; assume it is known to the environment).
2. Create the public `shiftpilot-ai` repository (description: "AI-assisted workload
   planning with human-reviewed extraction and deterministic scheduling."; topics:
   ai, llm, openrouter, react, typescript, fastify, sqlite, vite, pnpm, innovation-hacks)
   and push `main` with the README + docs.
3. Record the demo per `docs/demo-script.md` (2–4 min, fake mode, no credentials on
   screen) and upload it.
4. Publish the LinkedIn post from `docs/linkedin-post.md` (tag: Innovation Hacks) and
   complete the final submission with repo + video URLs.
